import java.util.*;
public class Mistakes {

    public static void main(String args[]){
        /*code qui renvoie une erreur de type ArrayIndexOutOfBoundsException
        int[] array = {0,1,2,3};
        int i = 0;
        while (i<=array.length){
            System.out.println(array[i]);
            i++;
        }
        //code qui renvoie une erreur de type NullPointerException
        String chiffre = null;
        System.out.println(chiffre.length());

        //code qui renvoie une erreur de type ConcurrentModificationException

        Vector<String> vect = new Vector<String>();
        vect.add("1");
        vect.add("2");
        vect.add("350");
        vect.add("10");
        vect.add("205");
        for (String str : vect) {
            if (str.length()>2) {
                vect.remove(str);
            }
        } */
    }


}
